Config = {}

-- From workspace car/ FF1 Menyoo pack (ModelHash 0xfc2e479a = kanjo)
-- These are vanilla GTA models styled via Menyoo — not streamed add-ons.
Config.DefaultModel = 'kanjo'

-- Cycle with /f3car <model> — F3 always uses current selection
Config.AllowedModels = {
    'kanjo',
    'jester',
    'remus',
    'penumbra',
    'rt3000',
    'dukes',
    'intruder',
    'zr350',
    'previon', -- Firebolt ASP-ish base if hash differs; fallback
}
